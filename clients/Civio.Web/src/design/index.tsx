// Design preview — redirects to public/Civio.html (Claude Design export).
// Place Civio.html from Claude Design into /public/Civio.html to enable.

export default function DesignPreview() {
  window.location.replace('/Civio.html')
  return null
}
